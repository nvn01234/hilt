version: '3.7'

services:

  trino:
    image: trinodb/trino:406
    ports:
      - 18080:8080
    volumes:
      - ./configurations:/etc/trino
      - ./etc:/etc/misc
      - trino_data:/data
    networks:
      - local

volumes:
  trino_data:

networks:
  local:
    external: true
