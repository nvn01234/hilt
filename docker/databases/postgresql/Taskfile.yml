version: '3'

tasks:

  start:
    cmds:
    - docker compose up -d --remove-orphans

  stop:
    cmds:
    - docker compose down --volumes

  shell:
    cmds:
    - docker compose exec -it postgresql psql postgresql://postgres:SuperSecr3t@postgresql:5432/postgres
